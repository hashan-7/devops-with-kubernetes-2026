apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-claim
  namespace: project
spec:
  storageClassName: my-local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi